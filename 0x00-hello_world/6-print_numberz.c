#include <stdio.h>
#include <stdlib.h>

/**
 * printing integers to base 10.
 * don't use printf & puts.
 * putchar only twice to be used
 * return 0.
 */
int main(void)
{
int i = 48;
for(i = 48; i <= 58; i++)
{
putchar(i);
}
putchar('\n');
return(0);
}
