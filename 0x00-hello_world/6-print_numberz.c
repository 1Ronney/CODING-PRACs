#include <stdio.h>
#include <stdlib.h>

/**
 * printing integers to base 10.
 * don't use printf & puts.
 * putchar only twice to be used
 * return 0.
 */
int main(void)
{
int i = 26;
for(i = 26; i <= 46; i++)
{
putchar(i);
}
putchar('\n');
return(0);
}
